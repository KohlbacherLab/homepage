import { TeamID } from "../..";
import { definePerson } from "../..";

export default definePerson({
    avatar: '/images/persons/default.png',
    name: 'Arslan Siraj',
    team: TeamID.ABI,
    role: 'PhD Student',
    socialLinks: [],
});
